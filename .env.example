# Copier ce fichier en `.env` (non versionné) avant de lancer l'application.

# Base de données (SQLite en développement, zéro configuration).
# Pour passer à PostgreSQL/Supabase : changer ce DSN + le datasource dans prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Correction automatique des réponses libres par IA — OPTIONNELLE.
# L'application fonctionne entièrement sans cette clé (auto-évaluation manuelle).
# AI_API_KEY=""
