# Copier ce fichier en `.env` (non versionné) avant de lancer l'application.

# Base de données PostgreSQL (Neon).
# DIRECT_URL = URL sans pooler (pour les migrations Prisma).
DATABASE_URL="postgresql://user:password@host/db?sslmode=require&channel_binding=require"
DIRECT_URL="postgresql://user:password@host/db?sslmode=require&channel_binding=require"

# Correction automatique des réponses libres par IA — OPTIONNELLE.
# AI_API_KEY=""
